version: 2
jobs:
  build:
    docker:
      - image: microsoft/aspnetcore-build:2.0
    steps:
      - checkout
      - setup_remote_docker
      - run: |          
          ./build.sh
workflows:
  version: 2
  build_push:
    jobs:
      - build:
          filters:
            branches:
              only: master